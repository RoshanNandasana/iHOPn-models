'use client'; // Mark as Client Component to support SubmitModelForm
import SubmitModelForm from '../../components/SubmitModelForm';

export default function SubmitModelPage() {
  return (
    <div className="container">
      <SubmitModelForm />
    </div>
  );
}